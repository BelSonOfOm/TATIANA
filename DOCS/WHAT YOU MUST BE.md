READ THIS AND YOU MUST BE THIS. TELL ME IF YOU CAN:

I've read through all three documents carefully — your plan, the detailed technical response (Phase 1 doc), and the more abstract architecture doc. Before I answer, let me check current facts on a few things this plan's economics depend on, since "free tier" details shift constantly and I don't want to advise you based on stale numbers.Assessed capabilities, recalibrated free-tier assumptions, architected phased implementation planAssessed capabilities, recalibrated free-tier assumptions, architected phased implementation planI've gone through all three — your plan, the detailed Phase 1–5 response, and the "Hopeful Architecture" doc. Here's the direct answer to what you actually asked first, then everything else that matters before we start building.
How capable am I of building this with you: most of it, genuinely. What's being described — hybrid retrieval (BM25 + dense embeddings + cross-encoder reranking), a local-first pipeline that escalates to a bigger model only when it needs to, agents that call research APIs and write C++ — is a well-understood pattern, not exotic research. I can write real, working code for nearly all of it: the extractors, the FAISS+SQLite knowledge base, the retrieval/reranking logic, the routing between your local model and whatever you escalate to, the WSL2/CMake project scaffolding, and the numerical side of your eigenvalue work specifically (Eigen for the linear algebra, Spectra or ARPACK for large sparse eigenproblems — directly the right tool for what you're doing). I'll explain what each piece does as we go rather than just dropping code, since you know the math but I won't assume deep CS background.
I can also just be a strong Mathematics Agent myself, right now, in this conversation — paste me a derivation or a section of a paper and I'll actually engage with it. For one-off checks that's often more reliable than the elaborate "escalate to a frontier model" machinery both docs describe — that machinery exists to solve a cost problem that only bites at real scale, not for you sending me one proof.
What I structurally am not, before we design around a wrong assumption: I'm not a free, local, always-on backend. I run on Anthropic's servers, not your GPU, and I don't persist between conversations by default — I won't remember your hard drive's contents or last month's theorem unless you show it to me again. I also can't run anything that depends on your actual GPU/CUDA/WSL2 setup — I have my own sandboxed Linux container for writing and sanity-checking code (I can test logic, run things on CPU, process sample text), but it isn't your machine. For anything GPU-dependent the loop is: I write it, you run it, you paste back what happened, I fix it. That's a real bottleneck on iteration speed, and better you know it now.
On the two technical documents themselves: the core architecture in the Phase 1–5 doc — hybrid retrieval, local draft, escalate only when stuck — is genuinely solid, standard engineering. I don't want the rest of this to read as "the whole plan is bad," because it isn't. But several of its specific numbers are the kind that sound rigorous without being measured: "~85% of Gemini quality," "spot 90% of logical errors automatically," "the rigor of a Fields-medal-level checker," "$0.000001" per call, "you will never hit the daily limit," "effectively infinite." Nobody benchmarked any of these against your actual work — they're confident-sounding placeholders, and I'd be doing exactly what I'm criticizing if I let you build a spec around them. Small telling detail: that document says the local 32B model is "exactly what I am based on" — it was written by DeepSeek-R1, recommending DeepSeek-R1 as your backend. Not necessarily wrong, but worth knowing whose confidence you're reading. The "Hopeful Architecture" doc is mostly generic agent-framework language (Perception/Cognition/Action/Memory/Infrastructure could describe almost any agent) — not wrong, just not adding much concrete beyond the other doc, except that it restates the same issue below in softer words.
One hard line — Sci-Hub and Anna's Archive: your own diagram routes to them directly, and the architecture doc's "Tier 3: Web Harvest… paywalled documentation from archive endpoints" is the same thing in euphemism. I won't help build the automated scraping/downloading piece targeting either — they host copyrighted material without authorization, and an agent that systematically pulls from them isn't a gray area. It costs you less than it sounds like, though: for pure math and mathematical physics, arXiv already carries nearly everything current as free preprints; Semantic Scholar (free API key) and Crossref cover metadata and citations; Unpaywall's API finds legal open-access copies of paywalled papers (author postprints, repository copies) for free. For whatever's left — an old textbook chapter, a paywalled journal-only version — your university library or interlibrary loan is the legitimate path, and I'll happily help you script against that if you have institutional access.
The free-tier numbers have already shifted, which proves the underlying point: as of April 2026, Google pulled Pro-series Gemini models off the free API tier entirely — Pro is paid-only now, which breaks the load-bearing assumption behind the entire Phase 1–5 cost model ("escalate to Gemini 2.5 Pro's free API"). What's still free is roughly the Flash/Flash-Lite tier, capped around 15 requests per minute and up to about 1,500 requests per day — different sources report slightly different exact numbers, and Google has cut these before without much notice, so check ai.google.dev/gemini-api/docs/rate-limits and your own AI Studio dashboard for live figures when we actually build this, not any number I give you today. OpenRouter's free tier is real and more stable in shape: 20 requests per minute, and either 50 or 1,000 requests per day depending on whether you've ever bought $10 of credits, across a rotating roster of genuinely useful open models — currently things like DeepSeek V4, Qwen3 Coder, and GPT-OSS-120B rather than the Llama 3.1 405B the doc named, which itself shows how fast the roster turns over. This is actually a point in favor of your own "interchangeable brain" instinct — just build the routing so swapping the escalation target is a one-line config change, because you'll be doing that regularly, not rarely. (For completeness: if you ever wanted me specifically as one of those swappable brains via API, that's metered like the others — Anthropic's API gives new keys a small starter credit but no lasting free tier for ongoing use.) Klymentiev + 5
The honest ceiling, underneath all of it: this pipeline will be genuinely good at retrieving and correctly assembling material that already exists in the literature — which is most of what "solve this eigenvalue problem on CP^3" actually means, since the Hodge theory involved is classical and sitting in Griffiths-Harris. That's real, valuable engineering; most people don't build good retrieval. It will not give you frontier-level reasoning on a genuinely open problem — something not already solved somewhere in a paper or book. No free or local setup does that reliably for anyone right now; that's not a flaw in your design, it's where the field is. A local model "stitching retrieved content into a perfect report" is pattern-completion over what's retrieved, not independent derivation — exactly right for your CP^3 example, exactly wrong for a genuinely new step in your own research. Practically: lean on the free/local stack hard for literature synthesis, code, and catching errors in your own writing — all real value — and treat any paid API calls as a small occasional budget for the handful of times a year you're actually stuck on something new, rather than architecting around never spending a cent. (And for the record — I haven't tried to verify the doc's specific claimed answer, λ = k(k+2n), for your CP^3 example. I'd want to actually work through it rather than nod along, and it's a good first real test whenever you want to run it, independent of any infrastructure.)
How I'd sequence the build (my default order — say if you want it differently):

Environment — WSL2, Python, verify GPU/CUDA is visible, git repo, folder layout.
Local knowledge base — extractors, embeddings, FAISS + SQLite, and the legitimate research agent (arXiv, Semantic Scholar, Crossref, Unpaywall).
Context builder — hybrid retrieval + reranking. Highest-leverage, most genuinely novel piece.
Escalation routing — a pluggable interface so Gemini/OpenRouter/local-Ollama (or me) are swappable behind one config, built expecting free-tier churn.
Code/Simulation agent — C++, CMake, Eigen/Spectra, hooked to local Ollama and the escalation layer.
Critique loop for your papers — chunk, hypothesis, targeted verification. The genuinely good idea from the Phase 1–5 doc, honestly scoped.
Memory/organization polish — auto-filing by subject and type, longer-term project memory.

For the IDE: I'd default to VS Code with the Remote-WSL extension over full Visual Studio — it's free, and it's the standard way to live inside a WSL2 Python+C++ stack without leaving Windows. Full Visual Studio's Linux workload is heavier and built more for cross-compiling from Windows than for living inside WSL2. Easy to change if you'd rather use real Visual Studio — just say so.
That's the preference-asking you wanted, and I'll keep doing it at every real fork, not just this one. Same for accountability: if I get something wrong as we go — a stale version, a bug, a mistaken claim — say so and I'll fix it and own it rather than hedge. This message is partly a demonstration of that: correcting confidently-stated numbers I can't stand behind is the same thing I'm on the hook for going forward.